# Contributing

Changes to the payment URI specification should include interoperable examples
and parser tests. Security-sensitive wallet behavior must fail closed rather
than silently interpreting ambiguous input.

Do not commit private keys, WIFs, seed phrases, RPC credentials or production
merchant secrets.
